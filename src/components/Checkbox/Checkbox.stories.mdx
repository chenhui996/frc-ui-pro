<!-- Button.stories.mdx-->

import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
// import Button from "./index";
import Checkbox from "./index";
import useDataDeal from "./useDataDeal";

<Meta title="数据录入/多选框 Checkbox" component={Checkbox} />

export const mockData = () => {
  const {
    plainOptions,
    options,
    optionsWithDisabled,
    checkedList,
    setCheckedList,
    indeterminate,
    setIndeterminate,
    checkAll,
    setCheckAll,
    onChange,
    onCheckAllChange,
  } = useDataDeal();
  return {
    plainOptions,
    options,
    optionsWithDisabled,
    checkedList,
    setCheckedList,
    indeterminate,
    setIndeterminate,
    checkAll,
    setCheckAll,
    onChange,
    onCheckAllChange,
  };
};

export const Template = (args) => <Checkbox {...args}>Checkbox</Checkbox>;

# Checkbox 多选框

多选框。

```ts
import { Checkbox } from "frc-ui-pro";
```

## 默认 Checkbox

<Canvas>
  <Story
    name="默认 Checkbox"
    args={{}}
    // 按需配置 argsTable
    argTypes={{
      autoFocus: {
        name: "autoFocus",
        description: "自动获取焦点",
        options: [true, false],
        control: {
          type: null,
        },
      },
      checked: {
        name: "checked",
        description: "指定当前是否选中",
        options: [true, false],
        control: {
          type: null,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## 不同状态 Checkbox

激活 | 禁用

<Canvas>
  <Story name="激活 | 禁用">
    <Checkbox>default</Checkbox>
    <Checkbox checked>checked</Checkbox>
    <br />
    <Checkbox disabled>disabled</Checkbox>
    <Checkbox checked disabled>
      checked disabled
    </Checkbox>
  </Story>
</Canvas>

## 复选框组 Group

方便的从数组生成 Checkbox 组。

<Canvas>
  <Story name="复选框组 Group ">
    <Checkbox.Group
      options={mockData().plainOptions}
      defaultValue={["Apple"]}
    />
    <br />
    <Checkbox.Group options={mockData().options} defaultValue={["Pear"]} />
    <br />
    <Checkbox.Group
      options={mockData().optionsWithDisabled}
      disabled
      defaultValue={["Apple"]}
    />
  </Story>
</Canvas>

```ts
// 数据源
const plainOptions = ["Apple", "Pear", "Orange"];
const options = [
  { label: "Apple", value: "Apple" },
  { label: "Pear", value: "Pear" },
  { label: "Orange", value: "Orange" },
];
const optionsWithDisabled = [
  { label: "Apple", value: "Apple" },
  { label: "Pear", value: "Pear" },
  { label: "Orange", value: "Orange", disabled: false },
];
```

## 全选 indeterminate

在实现全选效果时，你可能会用到 indeterminate 属性。

<Canvas>
  <Story name="test ">
    <Checkbox
      indeterminate={mockData().indeterminate}
      onChange={mockData().onCheckAllChange}
      checked={mockData().checkAll}
    >
      Check all
    </Checkbox>
    <br />
    <Checkbox.Group
      options={mockData().plainOptions}
      value={mockData().checkedList}
      onChange={mockData().onChange}
    />
  </Story>
</Canvas>

## API

通过设置 Checkbox 的属性来产生不同的多选框样式。

**按钮的属性说明如下：**

<ArgsTable of={Checkbox} />
